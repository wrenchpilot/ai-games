In the style of the Matrix movie screensaver, create an interactive terminal interface that simulates a digital rain effect. The interface should display random characters falling down the screen, with the ability to start and stop the effect using keyboard inputs. The characters should include letters, numbers, and symbols, and the speed of the falling characters should be adjustable. Also have a terminal prompt in the style of a command line interface where users can type commands. The interface should be visually appealing, with a dark background and green text, reminiscent of the Matrix aesthetic.

Additional features:
- Add a cyberpunk symbols mode that displays futuristic symbols instead of traditional Matrix characters
- Include a command to toggle between Matrix mode (green letters/Japanese characters) and symbols mode (cyberpunk symbols)
- Keep the green Matrix color scheme for both modes to maintain authenticity
- Add fullscreen mode functionality (F11 key and terminal command)
- Include a minimize button in the bottom left corner with a terminal icon
- Allow users to hide the terminal interface completely for full rain immersion
- Update the terminal interface to support the new mode switching and fullscreen commands
- Use authentic Matrix-style web fonts (Share Tech Mono and Orbitron)